# Scripts

This directory contains scripts used to perform the BRCA1/BRCA2 NGS variant analysis.

Scripts cover quality control, read preprocessing, alignment, BAM processing, variant calling, variant filtering, VEP annotation, and downstream analysis.
